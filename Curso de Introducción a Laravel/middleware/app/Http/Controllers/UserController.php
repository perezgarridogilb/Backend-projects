<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class UserController extends Controller
{
    /**
     * Primer método que se va a ejecutar cuando se intente acceder a este controlador
     * Si va a manipular usuarios necesita que este controlador este protegido con
     */
/*
    Esto se hace en las rutas para que los controladores no sean exageradamente largos
    public function __construct()
    {
        $this->middleware('auth');
    } */
}
